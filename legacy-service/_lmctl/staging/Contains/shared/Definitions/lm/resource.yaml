name: resource::shared-legacy-service::1.0
description: descriptor for infrastructure
properties:
  asterisk_ip:
    description: asterisk ip address
    read_only: true
  sipp_ip:
    description: sipp ip address
    read_only: true
lifecycle:
- Create
- Delete
